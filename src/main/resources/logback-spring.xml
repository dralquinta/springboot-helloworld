<configuration>

    <!-- Console appender for local debugging -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Google Cloud Logging appender -->
    <appender name="CLOUD" class="com.google.cloud.logging.logback.LoggingAppender">
        <log>projects/dryruns/logs/springboot-log</log> <!-- Replace with your GCP project ID -->
        <flushLevel>ERROR</flushLevel>
    </appender>

    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="Console" /> <!-- Logs to stdout locally -->
        <appender-ref ref="CLOUD" />   <!-- Sends logs to GCP Logging -->
    </root>

</configuration>
