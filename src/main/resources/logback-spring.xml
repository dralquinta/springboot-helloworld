<configuration>

    <!-- Console appender for local debugging -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Google Cloud Logging appender -->
    <appender name="CLOUD" class="com.google.cloud.logging.logback.LoggingAppender">
        <!-- Ensure the correct log name, replace YOUR_PROJECT_ID -->
        <log>projects/dryruns/logs/springboot-log</log> 
        <flushLevel>ERROR</flushLevel> <!-- Flush logs of ERROR level and above immediately -->
    </appender>

    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="Console" /> <!-- Logs to stdout for debugging -->
        <appender-ref ref="CLOUD" />   <!-- Sends logs to Google Cloud Logging -->
    </root>

</configuration>
