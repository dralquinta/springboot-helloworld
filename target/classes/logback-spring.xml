<configuration>

    <!-- Console appender for local debugging (outputs to stdout) -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Google Cloud Logging appender -->
    <appender name="CLOUD" class="com.google.cloud.logging.logback.LoggingAppender">
        <!-- Remove custom log name to default to stdout/stderr -->
        <flushLevel>ERROR</flushLevel> <!-- Flush immediately for ERROR and above -->
    </appender>

    <root level="INFO">
        <appender-ref ref="Console" /> <!-- Local console (stdout) logs for debugging -->
        <appender-ref ref="CLOUD" />   <!-- Sends logs to Google Cloud Logging -->
    </root>

</configuration>
